# User Service

Простой микросервис-заглушка для управления пользователями.  
Используется как эмулятор в интеграционных тестах и для локальной разработки.

- Хранит пользователей в памяти (`ConcurrentHashMap`)
- Валидирует email через `@Email`
- Обеспечивает уникальность email (возвращает `409 Conflict` при дубликате)
- Написан на **Java 17 + Spring Boot 3.4+**

---

## 🚀 Запуск

### Из исходников
```bash
./gradlew bootRun
```

### В Docker
Соберите образ:
```bash
docker build -t user-service .
```

Запустите контейнер:
```bash
docker run -p 8080:8080 user-service
```

Сервис будет доступен на: `http://localhost:8080`

---

## 📡 API

### Создать пользователя
```bash
curl -X POST http://localhost:8080/api/users \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com"}'
```
- **Успешно**: `HTTP 201 Created` + заголовок `Location`
- **Невалидный email**: `HTTP 400 Bad Request`
- **Дубликат email**: `HTTP 409 Conflict`

### Получить пользователя по email
```bash
curl http://localhost:8080/api/users/by-email?email=test@test.com
```
- **Найден**: `HTTP 200 OK` + JSON с полями `id` и `email`
- **Не найден**: `HTTP 404 Not Found`

### Получить пользователя по ID
```bash
curl http://localhost:8080/api/users/123e4567-e89b-12d3-a456-426614174000
```
- **Найден**: `HTTP 200 OK`
- **Не найден**: `HTTP 404 Not Found`

---

## 🧪 Тестирование

Проект включает интеграционные тесты на:
- Валидацию email
- Уникальность
- Создание и чтение пользователя

Запуск тестов:
```bash
./gradlew test
```

---

## 📦 Технологии

- **Язык**: Java 17
- **Фреймворк**: Spring Boot 3.4+
- **Сборка**: Gradle
- **Тесты**: JUnit 5, Mockito, MockMvc